<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.App"
        tools:targetApi="31">
        <activity
            android:name=".Activities.Settings.MiniSettingsActivity"
            android:exported="false" />
        <activity
            android:name=".Activities.Settings.SettingsActivity"
            android:exported="false" />
        <activity
            android:name=".Activities.Messages.AddNewChatActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.App">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.Conversations.ConversationsActivity"
            android:exported="true"
            android:theme="@style/Theme.App" />
        <activity
            android:name=".Activities.Messages.ChatActivity"
            android:exported="true"
            android:theme="@style/Theme.App" />
        <activity
            android:name=".Activities.Register.RegisterActivity"
            android:exported="true"
            android:theme="@style/Theme.App" />
    </application>

</manifest>